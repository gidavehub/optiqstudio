import StoryboardWizard from "../_views/StoryboardWizard";

// /dashboard/create — the storyboard configuration wizard (run-time, prompt,
// branding) plus the paywall/production-mode modal.
export default function CreatePage() {
  return <StoryboardWizard />;
}
