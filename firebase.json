{
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  },
  "storage": [
    {
      "bucket": "davelabs-tools",
      "rules": "storage.rules"
    },
    {
      "bucket": "davelabs-tools.firebasestorage.app",
      "rules": "storage.rules"
    }
  ],
  "database": {
    "rules": "database.rules.json"
  },
  "functions": [
    {
      "source": "functions",
      "codebase": "default",
      "runtime": "nodejs20"
    }
  ]
}
